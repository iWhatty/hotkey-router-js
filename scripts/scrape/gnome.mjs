// scripts/scrape/gnome.mjs
//
// Canonical source:
//   https://help.gnome.org/gnome-help/shell-keyboard-shortcuts.html
//
// Page structure: Mallard-XML rendered as <table class="table">. The page
// is shipped per-language; query the en-US variant explicitly.
//
// Out of scope for now: KDE and XFCE desktop reservations. The bundled data
// covers GNOME defaults only; document this as a known gap in the README.

export default async function scrapeGnome() {
  // TODO: implement parsing of GNOME shell shortcuts table.
  return null
}
